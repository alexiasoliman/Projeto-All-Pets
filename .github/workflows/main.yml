name: Node.js CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  jobs:
    build:
      name: Test
        matrix:
          node-version: [12.x, 14.x, 16.x]
          os: [ubuntu-latest]

steps:
  - run: http-server
